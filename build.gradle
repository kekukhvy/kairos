plugins {
    id 'java'
}

subprojects {
    apply plugin: 'java'

    java {
        toolchain {
            languageVersion = JavaLanguageVersion.of(javaToolchainVersion as int)
        }
    }

    repositories {
        mavenCentral()
    }
}