<configuration>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Keep Flyway info visible — useful to see which migrations applied -->
    <logger name="org.flywaydb" level="INFO"/>

    <!-- Kairos own code at DEBUG during development -->
    <logger name="dev.kairos" level="DEBUG"/>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

</configuration>
